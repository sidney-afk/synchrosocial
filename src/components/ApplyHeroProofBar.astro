<section class="apply-hero-proof" aria-label="Synchro Social proof points">
  <div class="apply-hero-proof__grid">
    <article class="apply-hero-proof__card">
      <p class="apply-hero-proof__label">Client Reach</p>
      <strong class="apply-hero-proof__metric">1.2B</strong>
      <p class="apply-hero-proof__copy">Views generated for our clients</p>
    </article>

    <article class="apply-hero-proof__card apply-hero-proof__card--award">
      <img
        src="/images/awards/european-agency-awards-2026-finalist-white.png"
        alt="European Agency Awards 2026"
        width="1024"
        height="680"
        loading="lazy"
        decoding="async"
      />
      <strong class="apply-hero-proof__award-status">2026 Finalist</strong>
      <p class="apply-hero-proof__copy">Social Media Agency of the Year</p>
    </article>

    <article class="apply-hero-proof__card">
      <p class="apply-hero-proof__label">Audience Growth</p>
      <strong class="apply-hero-proof__metric">8M</strong>
      <p class="apply-hero-proof__copy">Followers grown across platforms</p>
    </article>
  </div>
</section>

<style>
  .apply-hero-proof {
    width: min(840px, calc(100% - 48px));
    margin-inline: auto;
  }

  .apply-hero-proof__grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 12px;
  }

  .apply-hero-proof__card {
    display: flex;
    min-height: 152px;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 16px;
    border: 1px solid rgba(255, 255, 255, 0.18);
    border-radius: 4px;
    background: rgba(12, 8, 20, 0.72);
    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.06);
    text-align: center;
  }

  .apply-hero-proof__label,
  .apply-hero-proof__copy {
    margin: 0;
    font-family: "Inter", Arial, sans-serif;
  }

  .apply-hero-proof__label {
    color: #d8c4ff;
    font-size: 11px;
    font-weight: 700;
    line-height: 14px;
    letter-spacing: 0.13em;
    text-transform: uppercase;
  }

  .apply-hero-proof__metric,
  .apply-hero-proof__award-status {
    margin: 10px 0 7px;
    color: #fff;
    font-family: "Inter Tight", Arial, sans-serif;
    font-weight: 700;
    letter-spacing: 0;
  }

  .apply-hero-proof__metric {
    font-size: 44px;
    line-height: 44px;
  }

  .apply-hero-proof__award-status {
    font-size: 18px;
    line-height: 20px;
  }

  .apply-hero-proof__copy {
    color: rgba(255, 255, 255, 0.72);
    font-size: 13px;
    font-weight: 400;
    line-height: 18px;
  }

  .apply-hero-proof__card--award img {
    display: block;
    width: 134px;
    max-width: 76%;
    height: 52px;
    object-fit: contain;
  }

  .apply-hero-proof__card--award .apply-hero-proof__award-status {
    margin-top: 8px;
  }

  @media (max-width: 700px) {
    .apply-hero-proof {
      width: calc(100% - 32px);
    }

    .apply-hero-proof__grid {
      gap: 6px;
    }

    .apply-hero-proof__card {
      min-height: 126px;
      padding: 10px 5px;
      border-radius: 3px;
    }

    .apply-hero-proof__label {
      font-size: 8px;
      line-height: 11px;
      letter-spacing: 0.09em;
    }

    .apply-hero-proof__metric {
      margin: 8px 0 5px;
      font-size: 28px;
      line-height: 29px;
    }

    .apply-hero-proof__copy {
      max-width: 94px;
      font-size: 10px;
      line-height: 13px;
    }

    .apply-hero-proof__card--award img {
      width: 84px;
      max-width: 88%;
      height: 34px;
    }

    .apply-hero-proof__award-status {
      margin: 5px 0 3px;
      font-size: 13px;
      line-height: 14px;
    }
  }
</style>
